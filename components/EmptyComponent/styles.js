import {StyleSheet} from 'react-native';
import {MetricsMod} from '../../themes';

export default StyleSheet.create({
  imageContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    paddingVertical: MetricsMod.hundredEighty,
  },
  notFoundImage: {},
});
